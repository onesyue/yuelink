// updater — presentation layer placeholder (Phase 6 skeleton)
// TODO: implement update prompt UI (version info, changelog, download link)

import 'package:flutter/material.dart';

class UpdaterPage extends StatelessWidget {
  const UpdaterPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('updater — placeholder')),
    );
  }
}
