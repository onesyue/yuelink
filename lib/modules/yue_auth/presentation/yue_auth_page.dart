// yue_auth — presentation layer placeholder (Phase 6 skeleton)
// TODO: implement login/logout UI when YueApi and AuthRepository are ready

import 'package:flutter/material.dart';

class YueAuthPage extends StatelessWidget {
  const YueAuthPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('yue_auth — placeholder')),
    );
  }
}
