// yue_store — presentation layer placeholder (Phase 6 skeleton)
// TODO: implement store UI (plans, purchase entry, order status)

import 'package:flutter/material.dart';

class YueStorePage extends StatelessWidget {
  const YueStorePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('yue_store — placeholder')),
    );
  }
}
