// yue_account — presentation layer placeholder (Phase 6 skeleton)
// TODO: implement account profile, membership status, device list UI

import 'package:flutter/material.dart';

class YueAccountPage extends StatelessWidget {
  const YueAccountPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('yue_account — placeholder')),
    );
  }
}
