## Improvements

- **订阅套餐按钮右对齐** — 套餐卡片里的「续订 / 立即订阅」按钮现在固定贴齐卡片内容区右侧，价格仍保持左侧对齐，避免按钮在宽屏或长价格文本下偏到中间。
- **iOS TUN 分层诊断** — 连接修复页新增 iOS 专用 TUN 诊断面板，聚焦 iOS 用户真正可操作的 Core、DNS、目标站三层状态，不再套用桌面端 driver / route / admin 诊断模型。
- **支付链路更稳** — 修复购买 / 取消 / 轮询之间的异步竞态，空支付链接现在会明确失败，不再静默卡在等待支付。
- **连接修复页拆分** — 诊断日志、网络探测、修复动作和 TUN 状态 widget 已拆出独立模块，保留原视觉，同时补上 service/actions/widget 回归测试。
- **ServiceManager 探活补测试** — 桌面 helper 安装态、就绪态、IPC 可达性和平台分支增加测试切口；安装 / 卸载脚本本轮未重写。
- **Riverpod 3 迁移完成** — 全仓 `flutter_riverpod/legacy.dart` 清零，旧 `.notifier.state =` 写法清零，状态 provider 统一迁到 `NotifierProvider`。
- **ConfigTemplate 拆分** — 配置模板处理拆成 DNS / TUN / rules / scalar / provider-proxy 等 transformer，并新增 golden lock，保证重构前后输出稳定。
- **启动逻辑拆分** — `main.dart` 的设置加载、运行时 provider bootstrap、单实例守卫拆到 `lib/app/bootstrap/`，降低入口文件复杂度。
- **关键 UI 回归保护** — 新增 Mine 流量卡、套餐按钮对齐、页面玻璃背景、`TextAlign.end`、`YLRadius` token 使用等回归测试，覆盖最近几轮视觉收口容易回退的位置。

## Validation

- `flutter analyze` 通过。
- `flutter test` 通过：904 passed / 1 skipped。
- `bash scripts/check_imports.sh` 通过。
- `bash scripts/security_scan.sh` 通过：0 FAIL。
- `bash scripts/probe_telemetry.sh` 通过：5 个 telemetry dashboard endpoint 均返回 401 auth challenge。
- telemetry server unit tests 通过：42 tests。
- `git diff --check` 通过。

Local note: macOS integration build succeeded after setting UTF-8 locale, but local foreground launch was blocked by an already-running YueLink bundle. Treat GitHub Actions `Integration tests (macOS)` as the release-blocking result.

## Upgrade Notes

- 已登录用户无需重登，订阅与设置自动保留。
- 默认匿名遥测仍保持关闭；开启位置：设置 → 隐私 → 匿名遥测。
- 直接覆盖安装即可，所有平台。

## Known Limits

- 桌面 TUN 已继续增强诊断与状态一致性，但 24h soak 仍未完成；不要把本版本描述为 “TUN 已彻底稳定”。

## Downloads

| 平台 | 文件 | 说明 |
|---|---|---|
| Windows | `YueLink-1.1.10-windows-amd64-setup.exe` | 安装版 |
| Windows | `YueLink-1.1.10-windows-amd64-portable.zip` | 便携版 |
| macOS | `YueLink-1.1.10-macos-universal.dmg` | Universal（Apple Silicon + Intel） |
| Android | `YueLink-1.1.10-android-universal.apk` | 通用 |
| Android | `YueLink-1.1.10-android-arm64-v8a.apk` | arm64 |
| Android | `YueLink-1.1.10-android-armeabi-v7a.apk` | armv7 |
| Android | `YueLink-1.1.10-android-x86_64.apk` | x86_64 |
| iOS | `YueLink-1.1.10-ios.ipa` | 侧载，iOS 15+（TrollStore / AltStore / SideStore） |
| Linux | `YueLink-1.1.10-linux-amd64.AppImage` | x86_64 |

每个产物附同名 `.sha256`，汇总见 `YueLink-1.1.10-SHA256SUMS`。

**Full Changelog**: https://github.com/onesyue/yuelink/compare/v1.1.9...v1.1.10
