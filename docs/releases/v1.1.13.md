## Fixes

- **悦视频倍速状态修复** — 倍速选择现在区分“用户选择的常规倍速”和“长按临时 2x 加速”，点击屏幕或手势取消不会再把 1.25x / 1.5x / 2x 清回 1x。
- **悦视频倍速显示修复** — 速度面板勾选现在跟随用户实际选择的倍速，顶部倍速标签也会显示为 `2x` 而不是 `2.0x`。
- **Windows TUN 服务安装修复** — Windows 特权服务安装 / 更新 / 卸载脚本改为 UTF-8 BOM 写入，并通过 `EncodedCommand` 启动 elevated PowerShell，修复中文用户名或中文临时路径下安装服务失败的问题。

## Validation

- `flutter analyze --no-fatal-infos --no-fatal-warnings` 通过。
- `bash scripts/check_imports.sh` 通过。
- `flutter test` 通过：915 passed / 1 skipped。
- `git diff --check` 通过。

## Upgrade Notes

- 这是 v1.1.12 的 hotfix。已登录用户无需重登，订阅、设置和播放记录自动保留。
- Windows 用户升级后如仍显示“特权服务未安装”，请在设置 → 连接修复 → 桌面 TUN 中重新点击“安装服务”。

## Downloads

| 平台 | 文件 | 说明 |
|---|---|---|
| Windows | `YueLink-1.1.13-windows-amd64-setup.exe` | 安装版 |
| Windows | `YueLink-1.1.13-windows-amd64-portable.zip` | 便携版 |
| macOS | `YueLink-1.1.13-macos-universal.dmg` | Universal（Apple Silicon + Intel） |
| Android | `YueLink-1.1.13-android-universal.apk` | 通用 |
| Android | `YueLink-1.1.13-android-arm64-v8a.apk` | arm64 |
| Android | `YueLink-1.1.13-android-armeabi-v7a.apk` | armv7 |
| Android | `YueLink-1.1.13-android-x86_64.apk` | x86_64 |
| iOS | `YueLink-1.1.13-ios.ipa` | 侧载，iOS 15+（TrollStore / AltStore / SideStore） |
| Linux | `YueLink-1.1.13-linux-amd64.AppImage` | x86_64 |

每个产物附同名 `.sha256`，汇总见 `YueLink-1.1.13-SHA256SUMS`。

**Full Changelog**: https://github.com/onesyue/yuelink/compare/v1.1.12...v1.1.13
